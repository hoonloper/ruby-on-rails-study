<p style="color: green"><%= notice %></p>

<h1>Diaries</h1>

<div id="diaries">
  <% @diaries.each do |diary| %>
    <div>
      <%= link_to(diary) do %>
        <%= diary.title %>
      <% end %>
    </div>
  <% end %>
</div>

<%= link_to "New diary", new_diary_path %>
